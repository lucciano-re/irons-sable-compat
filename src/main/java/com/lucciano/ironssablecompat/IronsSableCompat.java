package com.yourname.ironssablecompat;

import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;

@Mod("ironssablecompat")
public class IronsSableCompat {
    public IronsSableCompat(IEventBus modEventBus) {
        // Nothing needed here - all the work is in the Mixin
    }
}
